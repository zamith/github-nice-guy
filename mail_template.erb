<p>Hello,</p>

<p>Here are some pull requests for <%= ENV['SEARCH_USER'] %> that you might want to give a look to:</p>

<ul>
  <% issues.items.map do |item| %>
    <li>
      <a href="<%= item.pull_request.html_url %>"><%= item.title %></a> by <%= item.user.login %>
    </li>
  <% end %>
</ul>

<p>The Github Nice Guy.</p>
